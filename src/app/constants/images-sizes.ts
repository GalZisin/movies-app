import { environment } from "../../environments/environment";

export const IMAGES_SIZES = {
    small: `${environment.imageBaseUrl}/w185`,
    medium: `${environment.imageBaseUrl}/w342`,
    large: `${environment.imageBaseUrl}/original`
}